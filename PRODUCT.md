# Yu Law 本地 AI 工作台产品规格

## 目标

在现有法律 AI 工具目录基础上，交付一个面向 Windows 的本地桌面软件。用户安装后可通过官方 Codex CLI 或 Claude Code 登录自己的 ChatGPT/Codex 或 Claude 账号，使用其已有订阅执行法律工作 Skill；原始文件、任务记录和结果默认保存在本机。

首个 Release Candidate 以三天内可安装、可演示、可实际完成任务为约束，不建设云端账户、远程服务器或新的模型 API 计费系统。

## [P0] Windows 本地桌面应用
- AC: 应用提供可启动的桌面壳、隔离的本地能力边界、中文主界面以及持久化用户数据目录。
- AC: 开发版本可完成核心旅程，并提供可复现的 Windows 安装包或绿色版构建配置与构建证据。
- AC: 普通用户运行交付物不依赖完整 Node.js 开发环境。

## [P0] 官方 Codex 与 Claude Code 订阅接入
- AC: 应用能检测官方 Codex CLI 与 Claude Code 的安装、版本和登录状态，且状态来自真实命令结果。
- AC: 用户可发起各 CLI 的官方登录流程并选择已登录的提供商执行任务。
- AC: 应用不读取、复制、输出或持久化浏览器 Cookie、OAuth token、账号密码或其他 CLI 凭据。
- AC: 应用不把网页订阅描述成通用 API 额度，并对安装缺失、未登录和不可用状态给出准确提示。

## [P0] 全量法律工具目录
- AC: `data/tools.json` 中的全部条目均可按分类、状态和关键字浏览，并展示输入、输出、步骤、版本、来源与风险提示。
- AC: 每个工具都显示由运行时判定的真实状态：可执行 Skill、本地网页、未安装或规划中。
- AC: localhost 工具可安全打开；不可达或未安装工具不得显示为可运行。

## [P0] 受控 Skill 任务执行
- AC: 用户可选择可执行 Skill、提供商、输入文件、任务说明和输出目录，并在开始前确认外发范围和隐私提示。
- AC: 官方 CLI 通过参数数组以非交互方式启动，不使用可注入的 Shell 字符串拼接；中文、空格和 Shell 特殊字符均被安全处理。
- AC: 执行过程提供实时状态、安全日志、取消和超时；成功后能打开成果，失败后能诊断和重试。
- AC: 至少一个已安装法律 Skill 分别通过已登录的 Codex 和 Claude Code 完成端到端任务并生成本地成果。

## [P0] 本地任务历史与成果管理
- AC: 本地持久化任务 ID、时间、工具、提供商、输入文件名、状态、成果路径和非敏感日志摘要。
- AC: 用户可筛选历史、打开成果目录、复制错误摘要、重试及删除单条历史记录。
- AC: 删除历史默认不删除原始文件或成果文件，任何文件删除均需独立明确确认。

## [P0] 安全与法律交付边界
- AC: 文件系统、子进程和桌面能力不直接暴露给不受信任的页面，IPC 与本地命令输入经过 schema 与路径校验。
- AC: 模型调用前明确展示提供商与拟发送文件，所有成果标注 AI 草稿并提示律师复核。
- AC: 凭据扫描证明代码、日志、数据库和构建产物不含 OAuth token、Cookie 或账号密码。
- AC: 应用不静默安装来源不明脚本，也不执行仓库中的任意命令。

## [P1] 完整桌面体验与可访问性
- AC: 首页提供新建任务、提供商状态、最近任务和工具分类，并覆盖空、加载、不可用、运行中、成功和失败状态。
- AC: 核心流程可用键盘操作，重要控件具有可访问名称，文本对比度满足桌面阅读需要。
- AC: 普通本地导航不重复确认，外发调用和危险操作具有清晰、适量的确认。

## [P0] 自动化质量与交付资料
- AC: 构建、类型检查、现有测试以及 provider 检测、命令构造、任务状态、持久化和关键 UI 测试全部通过。
- AC: README 包含面向普通用户的安装、登录、执行、找回成果说明，以及管理员故障排查与隐私说明。
- AC: 交付物包含第三方声明、软件物料信息、已知限制及可重复的 Windows 构建步骤。

### Journey: 律师安装并完成首个 AI 任务 -> Windows 本地桌面应用, 官方 Codex 与 Claude Code 订阅接入, 全量法律工具目录, 受控 Skill 任务执行, 本地任务历史与成果管理, 安全与法律交付边界

### Journey: 管理员验证并交付软件 -> 自动化质量与交付资料, Windows 本地桌面应用, 安全与法律交付边界

## 硬性边界

- 只通过官方 `codex` 与 `claude` CLI 的认证和非交互执行能力接入提供商。
- 不读取、复制、导出或保存浏览器 Cookie、CLI OAuth token、账号密码。
- 不声称 ChatGPT/Claude 网页订阅等同于通用 API 额度；界面应准确显示本机官方 CLI 的实际登录与可用状态。
- 默认本地处理。任务调用模型前明确显示将发送的文件和提供商，并要求用户主动开始。
- 法律成果必须标注为 AI 草稿并提示律师复核，不得表示为最终法律意见。
- 不自动安装来源不明的脚本，不静默执行仓库中的任意命令。

## 核心用户旅程

### 1. 安装与启动

- 用户可从 Windows 安装包安装，或使用绿色版启动。
- 软件首次启动自动创建本地工作目录、任务目录和结果目录。
- 软件可在没有 Node.js 开发环境的普通用户机器上启动。

### 2. 提供商登录与检查

- 设置页分别显示 Codex 与 Claude Code：是否安装、版本、是否登录、是否可执行。
- 未安装时提供可信说明和官方安装入口，不伪造成功状态。
- 未登录时可从软件发起官方 CLI 登录流程；登录过程由官方 CLI/浏览器完成。
- 已登录时可选择 Codex 或 Claude 作为任务提供商。
- 软件不展示或持久化任何 OAuth token。

### 3. 浏览全部法律工具

- 完整读取现有 `data/tools.json`，支持分类、状态、关键字搜索。
- 每个工具展示说明、输入、输出、步骤、风险提示、版本和来源。
- 工具必须有真实运行状态：可执行 Skill、本地网页、未安装或规划中；禁止把占位项显示成已可运行。
- 本地网页工具可从桌面端启动其 localhost 地址，并在不可达时给出可理解提示。

### 4. 发起本地 AI 任务

- 对可执行 Skill，用户可选择提供商、拖放或选择文件、填写任务说明、选择输出目录。
- 发起前展示确认摘要：提供商、Skill、文件、输出位置和隐私提示。
- 通过受控参数调用官方 CLI 的非交互模式，不经 Shell 字符串拼接，防止命令注入。
- 支持取消任务、超时、进程异常和提供商未登录错误。
- 实时展示状态和安全处理后的日志；不得把凭据写入日志。
- 成功后展示生成的文件并可打开结果目录；失败后保留可诊断信息并允许重试。

### 5. 任务历史与本地数据

- 本地持久化任务 ID、时间、工具、提供商、输入文件名、状态、结果路径和非敏感日志摘要。
- 可筛选历史任务、重新打开结果目录、复制错误摘要、删除单条历史记录。
- 删除历史记录不得默认删除用户原始文件或成果；涉及文件删除必须单独明确确认。
- 提供“打开数据目录”和隐私说明。

## 桌面体验

- 中文界面，适配常见 Windows 桌面尺寸。
- 首页突出“新建任务”、提供商状态、最近任务和工具分类，不照搬网站宣传页。
- 具备空状态、加载状态、不可用状态、运行中状态、成功和失败状态。
- 键盘可操作，重要控件有可访问名称，文本有足够对比度。
- 危险操作和外发模型调用有清晰确认，普通浏览和本地导航不反复打断。

## 技术与交付约束

- 优先复用现有 TypeScript/React UI 和工具数据，采用适合桌面打包的最小架构。
- 提供清晰的前端、桌面主进程/本地后端边界；文件系统和子进程能力不得直接暴露给不受信任的页面内容。
- 所有 IPC/本地命令输入均需 schema 校验和路径约束。
- 保持现有网页展示测试可运行；新增 provider 检测、命令构造、任务状态、持久化和关键 UI 流程测试。
- 提供开发启动、测试、构建、Windows 打包说明。
- 生成可审计的软件物料清单、许可证/第三方声明和已知限制。
- 若当前 Linux 构建机无法产出经过签名的 Windows 安装包，仍须产出可复现的 Windows 构建配置、CI 构建路径和本机可运行的桌面开发版本；不得把未生成的安装包标为已交付。

## Release Candidate 验收

- 工具目录中的全部项目均可浏览，并显示准确可用状态。
- 至少一个已安装法律 Skill 能分别通过已登录的 Codex 和 Claude Code 完成端到端任务，产生本地结果并写入历史。
- 提供商未安装、未登录、执行失败、取消和超时均有自动化测试或可重复验收证据。
- 参数中包含空格、中文和 Shell 特殊字符时不会发生命令注入或路径越界。
- 凭据扫描确认代码、日志、数据库和构建产物不包含 OAuth token 或账号密码。
- 构建、类型检查、单元/集成测试和现有测试全部通过。
- 桌面端可启动并完成核心用户旅程；Windows 构建物或可复现构建证据与限制说明齐全。
- README 提供非技术用户安装/登录/执行/找回结果说明，以及管理员故障排查说明。

## 非目标

- 自建 ChatGPT/Claude 登录页或代理登录服务。
- 绕过官方订阅限制、使用量限制或组织策略。
- 在三天版中实现每个规划中法律工具的独立业务算法。
- 云端同步、多人协作、律所权限系统、自动出具最终法律意见。
- 自动部署外部生产环境、代码签名证书采购或应用商店发布。
